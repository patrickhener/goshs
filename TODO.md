# Test
- Webhook + DNS + SMTP
- Test Windows and MacOS as well

# New implementation
- Error and Silent Page layout (claude)
- Maybe implement StartTLS and TLS for smtp collab (roadmap)
- Lightbox implementation for image mimetypes in file list (claude)
- Maybe theme integration
